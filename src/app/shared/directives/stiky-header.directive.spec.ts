import { StikyHeaderDirective } from './stiky-header.directive';

describe('StikyHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new StikyHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
